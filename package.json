{
  "name": "cherrfy-ecommerce",
  "version": "1.0.0",
  "description": "Root project to run admin and api together",
  "scripts": {
    "dev": "concurrently -n \"API,FRONTEND\" -c \"green,magenta,blue\" \"npm run dev --prefix api\" \"npm start --prefix frontend\""
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.4",
    "concurrently": "^9.1.2",
    "tailwindcss": "^3.4.17"
  },
  "dependencies": {
    "@reduxjs/toolkit": "^2.6.1",
    "@tailwindcss/cli": "^4.1.4",
    "react-redux": "^9.2.0"
  }
}
